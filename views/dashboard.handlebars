  <nav>
    <a href="/">Home</a> 
    {{#if loggedIn}}

  <a href="/logout">Logout</a>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>

{{#if loggedIn}}
<p>You are logged in dash</p>
{{else}}
<h2>Please log in dash</h2>
{{/if}}